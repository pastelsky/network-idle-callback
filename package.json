{
  "name": "network-idle-callback",
  "version": "1.0.1",
  "main": "lib/index.js",
  "license": "MIT",
  "dependencies": {},
  "files": [
    "lib/"
  ],
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  },
  "publishConfig":{ 
    "registry": "https://registry.npmjs.org"
  },
  "scripts": {
    "build": "babel index.js --out-file lib/index.js && babel request-monitor.js --out-file lib/request-monitor.js",
    "prepublish": "yarn build"
  }
}
